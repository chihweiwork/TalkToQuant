Run the TalkToQuant environment diagnostics to check all prerequisites for backtesting.

Execute this command:

```bash
bash /home/chihwei/playground/TalkToQuant/backtest/doctor.sh
```

Print the output directly. If the doctor exits with a non-zero code, help the user resolve the remaining issues listed in the output.

$ARGUMENTS
